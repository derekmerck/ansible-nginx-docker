galaxy_info:
  author: Derek Merck
  description: Configure and run a [Nginx](https://https://www.nginx.com) web server with reverse proxying in a Docker container.

  role_name: nginx-docker

  license: MIT

  min_ansible_version: 2.4

  galaxy_tags:
  - httpd
  - docker
  - nginx

dependencies:

  - role: geerlingguy.docker
    tags: deps
  - role: geerlingguy.pip
    pip_install_packages: docker
    tags: deps
